import SwiftUI

// Accessibility permissions are no longer required since we use
// NSRunningApplication.hide()/unhide() instead of AXUIElement APIs.
// This file is kept as an empty stub to satisfy the Xcode build target.
struct SetupView: View {
    var body: some View {
        EmptyView()
    }
}
